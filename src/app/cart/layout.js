export const metadata = {
    title: "Your Bag — Valemonte",
    description: "Review your selected items and proceed to checkout.",
};

export default function CartLayout({ children }) {
    return children;
}
